	DEFINE QLOCAL(BANKONE.REGISTER.IN)

	DEFINE QLOCAL(BANKONE.LOGIN.IN)
	
	DEFINE QLOCAL(THP.TRSMQ.BANKONE) USAGE(XMITQ)
		
	DEFINE QREMOTE(BANKONE.REGISTER.OUT) RNAME(THPBANKONE.REGISTER.IN) RQMNAME(QMTHP) XMITQ(THP.TRSMQ.BANKONE)

	DEFINE QREMOTE(BANKONE.LOGIN.OUT) RNAME(THPBANKONE.LOGIN.IN) RQMNAME(QMTHP) XMITQ(THP.TRSMQ.BANKONE)

	DEFINE CHANNEL(CHSDR.BANKONE.TO.THP) CHLTYPE(SDR) CONNAME('localhost(2412)') XMITQ(THP.TRSMQ.BANKONE)

	DEFINE CHANNEL(CHSDR.THP.TO.BANKONE) CHLTYPE(RCVR) 
	
	DEFINE LISTENER(QMBANKONELISNR) TRPTYPE(TCP) IPADDR('localhost') PORT(2421)

	START LISTENER(QMBANKONELISNR)
	
	START CHANNEL(CHSDR.BANKONE.TO.THP)

	START CHANNEL(CHSDR.THP.TO.BANKONE)
