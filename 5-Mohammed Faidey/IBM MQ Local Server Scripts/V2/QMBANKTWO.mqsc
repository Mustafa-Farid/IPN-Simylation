
	DEFINE QLOCAL(BANKTWO.REGISTER.IN)

	DEFINE QLOCAL(BANKTWO.LOGIN.IN)
	

	DEFINE QLOCAL(THP.TRSMQ.BANKTWO) USAGE(XMITQ)
		

	DEFINE QREMOTE(BANKTWO.REGISTER.OUT) RNAME(THPBANKTWO.REGISTER.IN) RQMNAME(QMTHP) XMITQ(THP.TRSMQ.BANKTWO)

	DEFINE QREMOTE(BANKTWO.LOGIN.OUT) RNAME(THPBANKTWO.LOGIN.IN) RQMNAME(QMTHP) XMITQ(THP.TRSMQ.BANKTWO)


	DEFINE CHANNEL(CHSDR.BANKTWO.TO.THP) CHLTYPE(SDR) CONNAME('localhost(2412)') XMITQ(THP.TRSMQ.BANKTWO)

	DEFINE CHANNEL(CHSDR.THP.TO.BANKTWO) CHLTYPE(RCVR) 
	

	DEFINE LISTENER(QMBANKTWOLISNR) TRPTYPE(TCP) IPADDR('localhost') PORT(2431)

	START LISTENER(QMBANKTWOLISNR)
	

	START CHANNEL(CHSDR.BANKTWO.TO.THP)

	START CHANNEL(CHSDR.THP.TO.BANKTWO)