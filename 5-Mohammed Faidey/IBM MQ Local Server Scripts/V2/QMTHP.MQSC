	DEFINE QLOCAL(THPBANKONE.REGISTER.IN)
	DEFINE QLOCAL(THPBANKTWO.REGISTER.IN)
	DEFINE QLOCAL(THPBANKTHREE.REGISTER.IN)	
	
	DEFINE QLOCAL(THPBANKONE.LOGIN.IN)
	DEFINE QLOCAL(THPBANKTWO.LOGIN.IN)
	DEFINE QLOCAL(THPBANKTHREE.LOGIN.IN)	
	

	DEFINE QLOCAL(THP.TRSMQ.BANKONE) USAGE(XMITQ)
	DEFINE QLOCAL(THP.TRSMQ.BANKTWO) USAGE(XMITQ)	
	DEFINE QLOCAL(THP.TRSMQ.BANKTHREE) USAGE(XMITQ)
	
	
	DEFINE QREMOTE(THPBANKONE.REGISTER.OUT) RNAME(BANKONE.REGISTER.IN) RQMNAME(QMBANKONE) XMITQ(THP.TRSMQ.BANKONE)
	DEFINE QREMOTE(THPBANKTWO.REGISTER.OUT) RNAME(BANKTWO.REGISTER.IN) RQMNAME(QMBANKTWO) XMITQ(THP.TRSMQ.BANKTWO)
	DEFINE QREMOTE(THPBANKTHREE.REGISTER.OUT) RNAME(BANKTHREE.REGISTER.IN) RQMNAME(QMBANKTHREE) XMITQ(THP.TRSMQ.BANKTHREE)

	DEFINE QREMOTE(THPBANKONE.LOGIN.OUT) RNAME(BANKONE.LOGIN.IN) RQMNAME(QMBANKONE) XMITQ(THP.TRSMQ.BANKONE)
	DEFINE QREMOTE(THPBANKTWO.LOGIN.OUT) RNAME(BANKTWO.LOGIN.IN) RQMNAME(QMBANKTWO) XMITQ(THP.TRSMQ.BANKTWO)
	DEFINE QREMOTE(THPBANKTHREE.LOGIN.OUT) RNAME(BANKTHREE.LOGIN.IN) RQMNAME(QMBANKTHREE) XMITQ(THP.TRSMQ.BANKTHREE)
	
			
	DEFINE CHANNEL(CHSDR.THP.TO.BANKONE) CHLTYPE(SDR) CONNAME('localhost(2421)') XMITQ(THP.TRSMQ.BANKONE)
	DEFINE CHANNEL(CHSDR.THP.TO.BANKTWO) CHLTYPE(SDR) CONNAME('localhost(2431)') XMITQ(THP.TRSMQ.BANKTWO)
	DEFINE CHANNEL(CHSDR.THP.TO.BANKTHREE) CHLTYPE(SDR) CONNAME('localhost(2441)') XMITQ(THP.TRSMQ.BANKTHREE)	
	

	DEFINE CHANNEL(CHSDR.BANKONE.TO.THP) CHLTYPE(RCVR) 
	DEFINE CHANNEL(CHSDR.BANKTWO.TO.THP) CHLTYPE(RCVR) 
	DEFINE CHANNEL(CHSDR.BANKTHREE.TO.THP) CHLTYPE(RCVR) 
	
	
	DEFINE LISTENER(QMTHP.LISNR) TRPTYPE(TCP) IPADDR('localhost') PORT(2412)

	START LISTENER(QMTHP.LISNR)
	
	START CHANNEL(CHSDR.THP.TO.BANKONE)
	START CHANNEL(CHSDR.THP.TO.BANKTWO)
	START CHANNEL(CHSDR.THP.TO.BANKTHREE)
	
	START CHANNEL(CHSDR.BANKONE.TO.THP)
	START CHANNEL(CHSDR.BANKTWO.TO.THP)
	START CHANNEL(CHSDR.BANKTHREE.TO.THP)
