
	DEFINE QLOCAL(THPBANKTWO.REGISTER.IN)

	DEFINE QLOCAL(THPBANKTWO.LOGIN.IN)
	

	DEFINE QLOCAL(THP.TRSMQ.BANKTWO) USAGE(XMITQ)

	
	DEFINE QREMOTE(THPBANKTWO.REGISTER.OUT) RNAME(BANKTWO.REGISTER.IN) RQMNAME(QMBANKTWO) XMITQ(THP.TRSMQ.BANKTWO)

	DEFINE QREMOTE(THPBANKTWO.LOGIN.OUT) RNAME(BANKTWO.LOGIN.IN) RQMNAME(QMBANKTWO) XMITQ(THP.TRSMQ.BANKTWO)
	
	
	DEFINE CHANNEL(CHSDR.THP.TO.BANKTWO) CHLTYPE(SDR) CONNAME('localhost(2431)') XMITQ(THP.TRSMQ.BANKTWO)

	DEFINE CHANNEL(CHSDR.BANKTWO.TO.THP) CHLTYPE(RCVR) 

	DEFINE LISTENER(QMTHPBANKTWOLISNR) TRPTYPE(TCP) IPADDR('localhost') PORT(2413)

	START LISTENER(QMTHPBANKTWOLISNR)
	

	START CHANNEL(CHSDR.THP.TO.BANKTWO)

	START CHANNEL(CHSDR.BANKTWO.TO.THP)
